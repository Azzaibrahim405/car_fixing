import 'package:flutter/material.dart';

class LlayOut extends StatelessWidget {
  const LlayOut({super.key});

  @override
  Widget build(BuildContext context) {
    // var translate = context.loc!;
    return const Placeholder();
  }
}